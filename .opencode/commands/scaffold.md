---
description: Scaffold a new BubbleTea model component
model: deepseek/deepseek-v4-flash
---

Create a new BubbleTea component named $1.

Load the bubbletea-patterns skill before generating any code.

Place the file at $2 (default: internal/ui/$1.go if not specified).
